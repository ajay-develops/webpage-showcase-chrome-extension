import type { ShowcaseGlobalConfig } from './types/showcase';

export const DEFAULT_CONFIG: ShowcaseGlobalConfig = {
  headerOffsetPx: 80,
  fastTransitionDurationMs: 1500,
  sectionScrollPixelsPerSecond: 150,
  minSectionScrollDurationMs: 2500,
  maxSectionScrollDurationMs: 14000,
  sectionStartPauseMs: 600,
  heroSectionStartPauseMs: 100,
  introDelayMs: 200,
  countdownSeconds: 1,
};
